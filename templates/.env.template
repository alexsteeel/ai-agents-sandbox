# Claude DevContainer Environment Configuration
# Copy this file to .env and customize for your project

# ============================================================================
# PROJECT CONFIGURATION
# ============================================================================

# Project name - used for container/volume naming (alphanumeric, hyphens only)
PROJECT_NAME=myproject

# Project directory - absolute path to your project root
PROJECT_DIR=/path/to/your/project

# ============================================================================
# PROXY CONFIGURATION (Optional)
# ============================================================================

# Upstream proxy for external connections (leave empty for direct connection)
# Formats: 
#   - HTTP proxy:   http://proxy.company.com:8080
#   - SOCKS5 proxy: socks5://proxy.company.com:1080
UPSTREAM_PROXY=

# Domains that should bypass the upstream proxy (space or comma separated)
# Only relevant when UPSTREAM_PROXY is set
NO_UPSTREAM=

# ============================================================================
# DOCKER IMAGE CUSTOMIZATION (Optional)
# ============================================================================

# Custom Dockerfile name (default: Dockerfile)
# Use this if you have a custom Dockerfile that extends the base image
DOCKERFILE=Dockerfile

# Base image to use (default: claudecode/devcontainer:latest)
# You can override this to use a different base image
BASE_IMAGE=claudecode/devcontainer:latest

# Build context directory (default: current directory)
BUILD_CONTEXT=.

# ============================================================================
# ADVANCED CONFIGURATION (Usually no need to change)
# ============================================================================

# Main proxy URL for devcontainer
PROXY_URL=http://tinyproxy-devcontainer:8888

# Docker-in-Docker proxy URL (for container registry access)
DIND_PROXY_URL=http://tinyproxy-dind:8888

# Whitelist files
WHITELIST_FILE=./whitelist.txt
DIND_WHITELIST_FILE=./dind-whitelist.txt

# Codex port binding (default: 1455)
CODEX_PORT=1455

# Workspace directory inside container
WORKSPACE=/workspace

# Override devcontainer command (default: sleep infinity)
# DEVCONTAINER_COMMAND=sleep infinity