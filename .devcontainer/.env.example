# Proxy configuration for devcontainer (uses upstream if configured)
DEVCONTAINER_HTTP_PROXY=http://tinyproxy-devcontainer:8888
DEVCONTAINER_HTTPS_PROXY=http://tinyproxy-devcontainer:8888
DEVCONTAINER_NO_PROXY=localhost,127.0.0.1,::1,docker,tinyproxy-devcontainer,tinyproxy-dind

# Proxy configuration for dind (no upstream, direct access only)
DIND_HTTP_PROXY=http://tinyproxy-dind:8888
DIND_HTTPS_PROXY=http://tinyproxy-dind:8888
DIND_NO_PROXY=localhost,127.0.0.1,::1,docker,tinyproxy-devcontainer,tinyproxy-dind

# Upstream proxy configuration (optional)
# Tinyproxy will forward requests through upstream proxy if configured
# Option 1: SOCKS5 proxy on host machine (uncomment to use)
#UPSTREAM_SOCKS5=host.docker.internal:8900

# Option 2: HTTP proxy (uncomment to use)
#UPSTREAM_HTTP=host.docker.internal:3128

# Option 3: Generic upstream proxy (uncomment and configure)
#UPSTREAM_PROXY_TYPE=socks5
#UPSTREAM_PROXY_HOST=host.docker.internal
#UPSTREAM_PROXY_PORT=1080

# Claude Code configuration
CLAUDE_CONFIG_DIR=/home/claude/.claude
NODE_OPTIONS=--max-old-space-size=4096