# Proxy configuration (shared by all services)
HTTP_PROXY=http://tinyproxy:8888
HTTPS_PROXY=http://tinyproxy:8888
NO_PROXY=localhost,127.0.0.1,::1,docker,tinyproxy
http_proxy=http://tinyproxy:8888
https_proxy=http://tinyproxy:8888
no_proxy=localhost,127.0.0.1,::1,docker,tinyproxy

# Upstream proxy configuration (optional)
# Tinyproxy will forward requests through upstream proxy if configured
# Option 1: SOCKS5 proxy on host machine (uncomment to use)
#UPSTREAM_SOCKS5=host.docker.internal:8900

# Option 2: HTTP proxy (uncomment to use)
#UPSTREAM_HTTP=host.docker.internal:3128

# Option 3: Generic upstream proxy (uncomment and configure)
#UPSTREAM_PROXY_TYPE=socks5
#UPSTREAM_PROXY_HOST=host.docker.internal
#UPSTREAM_PROXY_PORT=1080

# Docker configuration
DOCKER_HOST=tcp://docker:2376
DOCKER_TLS_VERIFY=1
DOCKER_CERT_PATH=/certs/client

# Claude Code configuration
CLAUDE_CONFIG_DIR=/home/claude/.claude
NODE_OPTIONS=--max-old-space-size=4096