# Allowed domains for firewall configuration
# One domain per line, empty lines and comments are ignored

# Note: Common tooling domains are provided by the base image in
# /usr/local/etc/default-allowed-domains.txt. Only add project-specific
# domains here.

# Add your custom domains below
gitlab.com

# Docker registries (needed for building images from within devcontainer)
docker.io
registry-1.docker.io
index.docker.io
auth.docker.io
hub.docker.com
production.cloudflare.docker.com
