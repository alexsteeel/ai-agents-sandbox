{
  "name": "${localWorkspaceFolderBasename} - Claude Code Sandbox",
  "dockerComposeFile": "docker-compose.yaml",
  "service": "devcontainer",
  "workspaceFolder": "/workspace",
  "shutdownAction": "stopCompose",
  "containerUser": "claude",
  "updateRemoteUserUID": false,
  "initializeCommand": "bash -c 'if [[ -f .devcontainer/init-project.sh ]]; then .devcontainer/init-project.sh; elif command -v ai-sbx-init-project >/dev/null 2>&1; then ai-sbx-init-project \"${localWorkspaceFolder}\"; fi'",
  "postCreateCommand": "/home/claude/scripts/non-root-post-create.sh",
  "remoteEnv": {
    "PROJECT_NAME": "${localWorkspaceFolderBasename}"
  }
}