# Project Configuration
# Only the minimal required settings that users need to customize

# Project name - used for container/volume naming (alphanumeric, hyphens only)
PROJECT_NAME=myproject

# Optional: Upstream proxy for external connections
# Formats: socks5://host:port or http://host:port
# UPSTREAM_PROXY=socks5://host.docker.internal:8900
# NO_UPSTREAM=github.com,gitlab.com